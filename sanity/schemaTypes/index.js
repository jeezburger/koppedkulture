import post from './post';
import product from './product';
import banner from './banner';
// Import other document schemas as needed

export const schema = {
  types: [
    post,
    product,
    banner
    // Add other document types here
  ]
};
